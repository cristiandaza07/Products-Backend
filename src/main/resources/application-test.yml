spring:
  servlet:
    multipart:
      max-file-size: 1MB
      max-request-size: 10MB
  cache:
    type: simple

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
  metrics:
    distribution:
      percentiles-histogram:
        http.server:
          requests: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html